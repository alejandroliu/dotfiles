#
# Screen rc with my preferred options
#
escape ``
#escape ^Gg

# Addidional key bindings
bind ^I other


# startup_message on|off
#  show or hide startup message
startup_message off

# Start windows from 1
bind c screen 1
bind ^c screen 1
bind 0 select 10
screen 1

# Enable 256 colors
term screen-256color

# defflow on|off|auto
#  manage the default flow control state for new windows
defflow off

	